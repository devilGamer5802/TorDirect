# WebSocket tracker list for browser WebTorrent (wss/ws only)
# Keep only WebSocket trackers (wss or ws) for browser compatibility.
# Comments (lines starting with #) are ignored.

wss://tracker.webtorrent.dev:443
wss://tracker.ghostchu-services.top:443/announce
wss://tracker.files.fm:7073/announce
wss://tracker.btorrent.xyz:443
# The following are ws (non-TLS) — on HTTPS pages these may be blocked. Prefer wss if available.
ws://tracker.ghostchu-services.top:80/announce
ws://tracker.files.fm:7072/announce

# Extra reliable public WebSocket trackers
wss://tracker.webtorrent.io
wss://tracker.openwebtorrent.com
wss://tracker.fastcast.nz
